1.   函数参数依次存放在 `a0` 到 `a7` 中。`main` 调用 `printf` 时，`a0` 存格式字符串的地址，`a1` 存 12，`a2` 存 13。
2.   `main` 中没有对 `f` 或 `g` 的调用。编译器将它们内联并直接算出了 `f(8)+1=12`。
3.   `printf` 位于地址 `0x6f6`。
4.   调用 `printf` 的指令位于 `0x30`，下一条指令位于 `0x34`，所以调用后 `ra` 中的值是 `0x34`。（此版本的反汇编将该调用显示为 `jal`。）
5.   输出为 `He110 World`。因为 57616 的十六进制是 `e110`；RISC-V 是小端序，所以 `i` 在内存中的字节依次是 `72 6c 64 00`，即字符串 `"rld"`。
6.   如果 RISC-V 是大端序，应将 `i` 设为 `0x726c6400`，这样内存中的字节仍是 `72 6c 64 00`。57616 不需要改变，因为整数参数本身的数值不受字节序影响。
7.   `y=` 后面的值不确定。格式字符串要求两个整数参数，但实际只传入了一个整数 3，`printf` 会把不存在的下一个参数当作整数读取，因此这是未定义行为，可能打印任意值。
